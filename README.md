# 🪐 Diskpart GUI (Space Neon)

**Diskpart GUI** — это графическая оболочка для стандартной утилиты Windows `diskpart`, позволяющая легко управлять дисками через удобный интерфейс.

![Превью](screenshot.png) <!-- Добавьте ваш скриншот здесь -->

---

## 🚀 Основные возможности

- Просмотр списка доступных дисков
- Выбор диска для работы
- Создание и удаление разделов: EFI, MSR, Windows, Recovery
- Форматирование разделов в NTFS или FAT32
- Конвертация диска в GPT или MBR
- Полное автоматическое разбиение диска одним нажатием
- Подсказки при наведении на каждую кнопку
- Подсветка кнопок при наведении
- Работает как в режиме Python, так и в собранном `.exe` файле

---

## ⚡ Описание кнопок

| Кнопка                     | Действие                                                         |
|-----------------------------|------------------------------------------------------------------|
| Показать диски              | Показывает список всех доступных дисков в системе               |
| Выбрать диск                | Выбирает диск для дальнейших операций                            |
| select disk                 | Выбирает указанный диск в `diskpart`                             |
| clean                       | Удаляет все разделы на выбранном диске                            |
| convert GPT                 | Конвертирует диск в GPT                                           |
| convert MBR                 | Конвертирует диск в MBR                                           |
| create partition primary    | Создает основной раздел                                           |
| create partition efi 300MB  | Создает EFI-раздел размером 300 МБ                                |
| create partition msr 16MB   | Создает MSR-раздел размером 16 МБ                                  |
| format NTFS                 | Форматирует раздел в NTFS с меткой `DiskX`                        |
| format FAT32                | Форматирует раздел в FAT32 с меткой `EFI`                         |
| assign letter               | Назначает букву S выбранному разделу                               |
| shrink 800MB                | Уменьшает размер раздела на 800 МБ                                |
| list volume                 | Показывает все тома                                               |
| detail disk                 | Показывает детальную информацию о диске                           |
| Разметка WIN                | Запускает автоматическое создание EFI, MSR, Windows и Recovery разделов |

---

## 🛠 Установка и запуск

### Запуск из Python
1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/ВашGitHub/Diskpart-GUI.git
Установите зависимости:

bash
Копировать код
pip install tk
Запустите скрипт:

bash
Копировать код
python main.py
Сборка в .exe через PyInstaller
bash
Копировать код
pyinstaller --noconsole --onefile --icon=logo.ico main.py
После сборки иконка и логотип сохраняются корректно.

⚠ Важно
Скрипт требует прав администратора, автоматически повышает права при запуске.

Будьте внимательны при использовании кнопок clean, format и Разметка WIN — они удаляют данные с выбранного диска.

Проверяйте правильный диск перед выполнением операций.

👤 Автор
꧁༺𝓒𝓲𝓬𝓪𝓭𝓪3301༻꧂

📄 Лицензия
Этот проект распространяется под лицензией MIT.

yaml
Копировать код

---

Если хотите, я могу **сразу добавить стильные эмодзи и подсветку для GitHub**, чтобы кнопки и таблицы выглядели еще более круто и ярко, в стиле вашего GUI.  

Хотите, чтобы я так сделал?
