# 🪐 Diskpart GUI (Space Neon)

**Diskpart GUI** — графическая оболочка для стандартной утилиты Windows `diskpart`, позволяющая легко управлять дисками через удобный и стильный интерфейс.

![Превью](screenshot.png)  


---

## 🚀 Основные возможности

✨ Просмотр списка доступных дисков  
🧭 Выбор диска для работы  
⚙️ Создание и удаление разделов ( EFI | MSR | Windows | Recovery )  
💾 Форматирование в NTFS или FAT32  
🔄 Конвертация диска в GPT или MBR  
🪄 Полная автоматическая разметка диска одним нажатием  
💡 Подсказки и подсветка при наведении на кнопки  
⚡ Работает как в Python-скрипте, так и в собранном `.exe`

---

## 🧭 Описание кнопок

| 🖲 Кнопка                     | 🧩 Действие                                                                 |
|-------------------------------|------------------------------------------------------------------------------|
| **Показать диски**           | Отображает все диски в системе                                             |
| **Выбрать диск**             | Устанавливает диск для операций                                            |
| **select disk**              | Выбирает указанный диск в `diskpart`                                       |
| **clean**                    | Полностью очищает диск (удаляет все разделы)                               |
| **convert GPT**              | Конвертирует диск в GPT                                                    |
| **convert MBR**              | Конвертирует диск в MBR                                                    |
| **create partition primary** | Создает основной раздел                                                    |
| **create partition efi 300MB** | Создает EFI-раздел размером 300 МБ                                        |
| **create partition msr 16MB** | Создает MSR-раздел размером 16 МБ                                         |
| **format NTFS**              | Форматирует раздел в NTFS (метка `DiskX`)                                  |
| **format FAT32**             | Форматирует раздел в FAT32 (метка `EFI`)                                   |
| **assign letter**            | Назначает букву `S` текущему разделу                                      |
| **shrink 800MB**             | Уменьшает размер раздела на 800 МБ                                        |
| **list volume**              | Отображает все тома                                                        |
| **detail disk**              | Показывает детальную информацию о диске                                   |
| **Разметка WIN**             | Автоматически создает EFI + MSR + Windows + Recovery разделы              |

---

## ⚙️ Установка и запуск

### 💻 Запуск из Python

```bash
git clone https://github.com/ВашGitHub/Diskpart-GUI.git
cd Diskpart-GUI
pip install tk
python main.py
```

---

### 🧩 Сборка в `.exe` через PyInstaller

```bash
pyinstaller --noconsole --onefile --icon=logo.ico main.py
```

После сборки иконка и логотип сохраняются корректно.  
Скрипт автоматически запрашивает **права администратора** при запуске.

---

## ⚠️ Важно

> 🚨 Будьте внимательны при использовании команд `clean`, `format` и **Разметка WIN** —  
> они **удаляют все данные** на выбранном диске!  
>  
> ✅ Перед выполнением проверьте, что указан нужный диск.

---

## 👤 Автор

**꧁༺𝓒𝓲𝓬𝓪𝓭𝓪3301༻꧂**

---

## 📄 Лицензия

Этот проект распространяется под лицензией **MIT**.  
Свободно используйте, изменяйте и распространяйте с указанием автора.
